info:
  name: login
  type: http

http:
  method: POST
  url: http://127.0.0.1:8001/api/login/
  body:
    type: json
    data: |-
      {
        "username": "usern",
        "password": "usern"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
​